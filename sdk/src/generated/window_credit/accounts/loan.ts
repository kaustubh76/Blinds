/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { assertAccountExists, assertAccountsExist, combineCodec, decodeAccount, fetchEncodedAccount, fetchEncodedAccounts, fixDecoderSize, fixEncoderSize, getAddressDecoder, getAddressEncoder, getBooleanDecoder, getBooleanEncoder, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, getU64Decoder, getU64Encoder, getU8Decoder, getU8Encoder, transformEncoder, type Account, type Address, type EncodedAccount, type FetchAccountConfig, type FetchAccountsConfig, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type MaybeAccount, type MaybeEncodedAccount, type ReadonlyUint8Array } from '@solana/kit';

export const LOAN_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([20, 195, 70, 117, 165, 227, 182, 1]);

export function getLoanDiscriminatorBytes(): ReadonlyUint8Array { return fixEncoderSize(getBytesEncoder(), 8).encode(LOAN_DISCRIMINATOR); }

export type Loan = { discriminator: ReadonlyUint8Array; lender: Address; borrower: Address; epoch: bigint; 
/** The clearing rate tick. */
tick: number; 
/** The borrower's bid tick (≥ `tick`). */
bidTick: number; k: number; 
/** `LoanStatus` discriminant. */
status: number; collateralReleased: boolean; 
/** Disclosed marginal ratio for this lender's fill (1/1 for a full fill). */
fillNum: bigint; fillDen: bigint; 
/** Grouped (borrower, auditor) ciphertext of the loan size — never plaintext. */
sizeCt: ReadonlyUint8Array; 
/** Grouped (borrower, auditor) ciphertext of the collateral shares — never plaintext. */
collateralCt: ReadonlyUint8Array; 
/** Pedersen commitment to Δ recorded at lock. */
deltaCommitment: ReadonlyUint8Array; 
/** For partial fills: the size ciphertext's opening, sealed to the borrower. Zero for full fills. */
openingNote: ReadonlyUint8Array; kC: bigint; kL: bigint; priceAtLock: bigint; multAtLock: bigint; lockSlot: bigint; fundedSlot: bigint; deadlineSlot: bigint; bump: number; 
/**
 * The `Listing` the collateral was locked under (default until `lock_collateral`). Appended
 * after `bump` so every earlier offset is unchanged; pre-listing loans are 32 bytes shorter
 * and are brought to this layout by `migrate_loan`.
 */
listing: Address;  };

export type LoanArgs = { lender: Address; borrower: Address; epoch: number | bigint; 
/** The clearing rate tick. */
tick: number; 
/** The borrower's bid tick (≥ `tick`). */
bidTick: number; k: number; 
/** `LoanStatus` discriminant. */
status: number; collateralReleased: boolean; 
/** Disclosed marginal ratio for this lender's fill (1/1 for a full fill). */
fillNum: number | bigint; fillDen: number | bigint; 
/** Grouped (borrower, auditor) ciphertext of the loan size — never plaintext. */
sizeCt: ReadonlyUint8Array; 
/** Grouped (borrower, auditor) ciphertext of the collateral shares — never plaintext. */
collateralCt: ReadonlyUint8Array; 
/** Pedersen commitment to Δ recorded at lock. */
deltaCommitment: ReadonlyUint8Array; 
/** For partial fills: the size ciphertext's opening, sealed to the borrower. Zero for full fills. */
openingNote: ReadonlyUint8Array; kC: number | bigint; kL: number | bigint; priceAtLock: number | bigint; multAtLock: number | bigint; lockSlot: number | bigint; fundedSlot: number | bigint; deadlineSlot: number | bigint; bump: number; 
/**
 * The `Listing` the collateral was locked under (default until `lock_collateral`). Appended
 * after `bump` so every earlier offset is unchanged; pre-listing loans are 32 bytes shorter
 * and are brought to this layout by `migrate_loan`.
 */
listing: Address;  };

/** Gets the encoder for {@link LoanArgs} account data. */
export function getLoanEncoder(): FixedSizeEncoder<LoanArgs> {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)], ['lender', getAddressEncoder()], ['borrower', getAddressEncoder()], ['epoch', getU64Encoder()], ['tick', getU8Encoder()], ['bidTick', getU8Encoder()], ['k', getU8Encoder()], ['status', getU8Encoder()], ['collateralReleased', getBooleanEncoder()], ['fillNum', getU64Encoder()], ['fillDen', getU64Encoder()], ['sizeCt', fixEncoderSize(getBytesEncoder(), 96)], ['collateralCt', fixEncoderSize(getBytesEncoder(), 96)], ['deltaCommitment', fixEncoderSize(getBytesEncoder(), 32)], ['openingNote', fixEncoderSize(getBytesEncoder(), 32)], ['kC', getU64Encoder()], ['kL', getU64Encoder()], ['priceAtLock', getU64Encoder()], ['multAtLock', getU64Encoder()], ['lockSlot', getU64Encoder()], ['fundedSlot', getU64Encoder()], ['deadlineSlot', getU64Encoder()], ['bump', getU8Encoder()], ['listing', getAddressEncoder()]]), (value) => ({ ...value, discriminator: LOAN_DISCRIMINATOR }));
}

/** Gets the decoder for {@link Loan} account data. */
export function getLoanDecoder(): FixedSizeDecoder<Loan> {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)], ['lender', getAddressDecoder()], ['borrower', getAddressDecoder()], ['epoch', getU64Decoder()], ['tick', getU8Decoder()], ['bidTick', getU8Decoder()], ['k', getU8Decoder()], ['status', getU8Decoder()], ['collateralReleased', getBooleanDecoder()], ['fillNum', getU64Decoder()], ['fillDen', getU64Decoder()], ['sizeCt', fixDecoderSize(getBytesDecoder(), 96)], ['collateralCt', fixDecoderSize(getBytesDecoder(), 96)], ['deltaCommitment', fixDecoderSize(getBytesDecoder(), 32)], ['openingNote', fixDecoderSize(getBytesDecoder(), 32)], ['kC', getU64Decoder()], ['kL', getU64Decoder()], ['priceAtLock', getU64Decoder()], ['multAtLock', getU64Decoder()], ['lockSlot', getU64Decoder()], ['fundedSlot', getU64Decoder()], ['deadlineSlot', getU64Decoder()], ['bump', getU8Decoder()], ['listing', getAddressDecoder()]]);
}

/** Gets the codec for {@link Loan} account data. */
export function getLoanCodec(): FixedSizeCodec<LoanArgs, Loan> {
    return combineCodec(getLoanEncoder(), getLoanDecoder());
}

export function decodeLoan<TAddress extends string = string>(encodedAccount: EncodedAccount<TAddress>): Account<Loan, TAddress>;
export function decodeLoan<TAddress extends string = string>(encodedAccount: MaybeEncodedAccount<TAddress>): MaybeAccount<Loan, TAddress>;
export function decodeLoan<TAddress extends string = string>(encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>): Account<Loan, TAddress> | MaybeAccount<Loan, TAddress> {
  return decodeAccount(encodedAccount as MaybeEncodedAccount<TAddress>, getLoanDecoder());
}

export async function fetchLoan<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Loan, TAddress>> {
  const maybeAccount = await fetchMaybeLoan(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeLoan<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Loan, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeLoan(maybeAccount);
}

export async function fetchAllLoan(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Loan>[]> {
  const maybeAccounts = await fetchAllMaybeLoan(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeLoan(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Loan>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeLoan(maybeAccount));
}

export function getLoanSize(): number {
  return 446;
}