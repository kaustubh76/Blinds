# Rust / Anchor
target/
.anchor/
**/*.rs.bk
# Program deploy keys are NOT committed: programs are immutable after the submission tag and the
# keys are only needed by the deployer. Keep them in a password manager; docs/toolchain.md.
deployments/program-keypairs/
# Node
node_modules/
dist/
.turbo/
*.tsbuildinfo
# sdk/wasm is COMMITTED: it is the browser proof engine, and committing it lets the dashboard be
# built and deployed (Vercel, a clean clone) without a Rust toolchain or wasm-pack. Regenerate with
# ./scripts/build_wasm.sh — the source of truth is crates/window-proofs-wasm.
# Env / secrets
.env
.env.*
!.env.example
*.pem
# OS / editors
.DS_Store
.idea/
.vscode/
# Local run artefacts
test-ledger/
*.log
services/indexer/data/
deployments/external/*.so

# Simulated-agent memory (bid sizes and openings): secret material by definition, and machine-written.
services/admin/data/
