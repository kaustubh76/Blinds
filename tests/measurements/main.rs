//! Cost measurements. Every number here is asserted (regression) and, with
//! `WINDOW_WRITE_MEASUREMENTS=1`, written to `docs/measurements.json` for the README.
mod gate_pocd;
mod print_cost;
mod report;
