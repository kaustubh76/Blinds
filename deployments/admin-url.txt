# The public URL of the admin service (demo faucet) while the market runs, one line, or nothing.
# `scripts/market.sh start` writes it (a Cloudflare quick tunnel); `stop` clears it;
# `scripts/publish_admin_url.sh` commits it so the hosted dashboard picks it up as a fallback
# to the `?admin=` link. Comment lines are ignored.
https://cannon-returned-composed-realm.trycloudflare.com
